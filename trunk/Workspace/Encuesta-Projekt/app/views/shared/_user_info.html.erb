<div class="user_info">
  <a href="<%= user_path(current_user) %>">
   <%= #gravatar_for current_user, :size => 30 
   %>
    <span class="user_login">
      <%= current_user.email %> hat
    </span>
    <span class="survey">
      <%= pluralize(current_user.surveys.count, "Umfrage(n)") 
      %>
    </span>
  </a>
</div>
